<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reja va Maqsadlar</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
      .btn {
        border-radius: 20px;
      }
      .form-container {
        display: flex;
        align-items: center;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="mb-4 text-center">REJA VA MAQSADLAR</h1>

      <div class="p-4 shadow-sm bg-white" style="border-radius: 15px;">
        <form
          id="create-form"
          action="/create-item"
          method="POST"
          class="form-container"
        >
          <input
            id="create-field"
            name="reja"
            type="text"
            class="form-control mr-3"
            style="flex: 1"
            autocomplete="off"
            autofocus
            required
          />
          <button class="btn btn-primary">Yangi Reja qo'shish</button>
        </form>
      </div>

      <ul id="item-list" class="list-group mt-4 pb-5">
        <% items.map(function(item) { %>
          <li class="list-group-item list-group-item-info d-flex align-items-center justify-content-between mb-2 shadow-sm">
            <span class="item-text"><%= item.reja %></span>
            <div>
              <button
                data-id="<%= item._id %>"
                class="edit-me btn btn-secondary btn-sm mr-1"
              >
                O'zgartirish
              </button>
              <button 
                data-id="<%= item._id %>" 
                class="delete-me btn btn-danger btn-sm"
              >
                O'chirish
              </button>
            </div>
          </li>
        <% }) %>
      </ul>

      <div class="text-right">
        <button id="clean-all" class="btn btn-danger mt-3">
          Hamma Rejani o'chirish
        </button>
      </div>
    </div>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="/browser.js"></script>
    </body>
</html>